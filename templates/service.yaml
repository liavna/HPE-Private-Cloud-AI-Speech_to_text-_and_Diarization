apiVersion: v1
kind: Service
metadata:
  name: {{ include "speech-ui.fullname" . }}
  labels:
    {{- include "speech-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "speech-ui.selectorLabels" . | nindent 4 }}
